import SectionTitle from "../../../../Shared/SectionTitle/SectionTitle";

const CouponManagement = () => {
    return (
        <div>
            <SectionTitle heading={"coupon "}></SectionTitle>
        </div>
    );
};

export default CouponManagement;