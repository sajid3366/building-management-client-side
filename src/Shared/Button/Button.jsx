
const Button = ({title}) => {
    return (
        <div>
            <p className="py-2 px-4 rounded-sm text-white cursor-pointer bg-[#12486B]">{title}</p>
        </div>
    );
};

export default Button;